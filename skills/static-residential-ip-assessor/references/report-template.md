# Report Template

```markdown
# IP 静态家宽与风险评估报告

**目标：** `<IP 或 CIDR>`  
**报告时间：** `<YYYY-MM-DD HH:mm TZ>`  
**调查目标：** `<属性判定 / 风险判定 / 双目标>`

---

## 一、最终结论（Executive Summary）

- **静态家宽属性结论：** `<是 / 否 / 不确定>`
- **属性置信度：** `<0-100>`（`<高/中/低>`）
- **风控风险结论：** `<低 / 中 / 高 / 极高>`
- **风险分数：** `<0-100>`
- **执行动作：** `<Allow / Review / Reject>`
- **IPQS 可用性：** `<可用 / 不可用>`
- **一句话说明：** `<结论摘要>`

---

## 二、输入信息

| 字段 | 值 |
|---|---|
| IP / 段 | `<value>` |
| ASN | `<value>` |
| Hostname | `<value>` |
| Range | `<value>` |

---

## 三、核心证据表

| 证据项 | 观测值 | 影响 | 来源 | 观测时间 |
|---|---|---|---|---|
| RDAP/WHOIS | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |
| BGP Origin ASN | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |
| PTR/rDNS | `<value>` | 支持/反对/中立 | `<command or url>` | `<time>` |
| IPQS | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |
| Scamalytics | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |
| AbuseIPDB | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |
| ipinfo/ip-api | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |

---

## 四、冲突分析

1. **冲突点：** `<例如：住宅运营商 vs 高代理评分>`
2. **可能原因：** `<算法差异/时效差异/段级污染等>`
3. **解释结论：** `<并存结论说明>`

---

## 五、评分明细

### 5.1 Static Residential Confidence（0-100）

| 维度 | 得分 | 说明 |
|---|---|---|
| ASN 与运营商属性 | `<x/25>` | `<why>` |
| BGP 与前缀一致性 | `<x/15>` | `<why>` |
| PTR 与相邻命名稳定性 | `<x/20>` | `<why>` |
| hosting/proxy/mobile | `<x/15>` | `<why>` |
| 段级声誉与代理渗透 | `<x/15>` | `<why>` |
| 数据完整性与时效 | `<x/10>` | `<why>` |
| **合计** | `<x/100>` | `<label>` |

### 5.2 Abuse / Block Risk（0-100）

| 维度 | 得分 | 说明 |
|---|---|---|
| IPQS Fraud Score | `<x/40>` | `<why>` |
| IPQS 代理标签 | `<x/25>` | `<why>` |
| AbuseIPDB 举报 | `<x/20>` | `<why>` |
| 多源一致性放大 | `<x/15>` | `<why>` |
| **合计** | `<x/100>` | `<label>` |

---

## 六、场景化建议

| 场景 | 建议 |
|---|---|
| 登录/注册 | `<建议>` |
| 支付/金融 | `<建议>` |
| 账号养号 | `<建议>` |
| 账号安全巡检 | `<建议>` |

---

## 七、执行动作依据（保守策略）

1. **触发规则：** `<红线/灰区/放行条件>`
2. **关键原因：** `<最多 3 条>`
3. **立即动作：** `<更换IP / 二次验证 / 暂缓关键操作>`
4. **二次验证状态：** `<未执行 / 通过 / 未通过>`
5. **CIDR 灰度状态：** `<未触发 / 已触发-降级Review>`
6. **IPQS 审计状态：** `<已记录关键字段+时间 / 缺失>`

---

## 八、不确定性与复查建议

- 当前缺失数据：`<list>`
- 结论受影响点：`<list>`
- 下次复查时间：`<建议日期，默认14天后>`

---

*免责声明：本报告基于公开数据源与时点观测，不构成法律意见。*
```
