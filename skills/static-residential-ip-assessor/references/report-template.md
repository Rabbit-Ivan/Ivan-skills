# Report Template

```markdown
# IP 静态家宽与风险评估报告

**目标：** `<IP 或 CIDR>`  
**报告时间：** `<YYYY-MM-DD HH:mm TZ>`  
**评分模式：** `<正常 / ⚠️ 降级（IPQS 不可用）>`

---

## 一、最终结论（Executive Summary）

- **静态家宽属性结论：** `<是 / 否 / 不确定>`
- **属性置信度：** `<0-100>`（`<高/中/低>`）
- **风控风险结论：** `<低 / 中 / 高 / 极高>`
- **风险分数：** `<0-100>`
- **执行动作：** `<Allow / Review / Reject>`
- **一句话说明：** `<结论摘要>`

---

## 二、数据源可用性

| 数据源 | 状态 | 备注 |
|---|---|---|
| ipinfo / ip-api | `<✅ 成功 / ❌ 失败 / ⏭️ 跳过>` | `<备注>` |
| AbuseIPDB | `<✅ / ❌ / ⏭️>` | `<备注>` |
| Scamalytics | `<✅ / ❌ / ⏭️>` | `<备注>` |
| IPQS | `<✅ / ❌ / ⏭️>` | `<备注>` |
| BGP (HE) | `<✅ / ❌ / ⏭️>` | `<备注>` |
| RDAP | `<✅ / ❌ / ⏭️>` | `<备注>` |
| PTR/rDNS | `<✅ / ❌ / ⏭️>` | `<备注>` |

---

## 三、输入信息

| 字段 | 值 |
|---|---|
| IP / 段 | `<value>` |
| ASN | `<value>` |
| Hostname | `<value>` |
| Range | `<value>` |

---

## 四、核心证据表

| 证据项 | 观测值 | 影响 | 来源 | 观测时间 |
|---|---|---|---|---|
| RDAP/WHOIS | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |
| BGP Origin ASN | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |
| PTR/rDNS | `<value>` | 支持/反对/中立 | `<command or url>` | `<time>` |
| IPQS | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |
| Scamalytics | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |
| AbuseIPDB | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |
| ipinfo/ip-api | `<value>` | 支持/反对/中立 | `<url>` | `<time>` |

---

## 五、冲突分析

1. **冲突点：** `<例如：住宅运营商 vs 高代理评分>`
2. **可能原因：** `<算法差异/时效差异/段级污染等>`
3. **解释结论：** `<并存结论说明>`

---

## 六、评分明细

### 6.1 Static Residential Confidence（0-100）

| 维度 | 得分 | 说明 |
|---|---|---|
| ASN 与运营商属性 | `<x/25>` | `<why>` |
| BGP 与前缀一致性 | `<x/15>` | `<why>` |
| PTR 与相邻命名稳定性 | `<x/20>` | `<why>` |
| hosting/proxy/mobile | `<x/15>` | `<why>` |
| 段级声誉与代理渗透 | `<x/15>` | `<why>` |
| 数据完整性与时效 | `<x/10>` | `<why>` |
| **合计** | `<x/100>` | `<label>` |

### 6.2 Abuse / Block Risk（0-100）

| 维度 | 得分 | 说明 |
|---|---|---|
| IPQS Fraud Score | `<x/40>` | `<why>` |
| IPQS 代理标签 | `<x/25>` | `<why>` |
| AbuseIPDB 举报 | `<x/20>` | `<why>` |
| 多源一致性放大 | `<x/15>` | `<why>` |
| **合计** | `<x/100>` | `<label>` |

> 若为降级评分模式，此表使用降级权重（见 scoring-rubric.md §4）。

---

## 七、账号安全建议

- **执行动作：** `<Allow / Review / Reject>`
- **触发规则：** `<红线/灰区/放行条件>`
- **关键原因：** `<最多 3 条>`
- **立即动作：** `<更换IP / 二次验证 / 暂缓关键操作 / 可安全使用>`
- **二次验证状态：** `<未执行 / 通过 / 未通过>`
- **历史记录参考：** `<无历史 / 上次评估日期与结果 / 同段风险记录>`

---

## 八、不确定性与复查建议

- 当前缺失数据：`<list>`
- 结论受影响点：`<list>`
- 下次复查时间：`<建议日期，默认14天后>`

---

*免责声明：本报告基于公开数据源与时点观测，不构成法律意见。*
```
