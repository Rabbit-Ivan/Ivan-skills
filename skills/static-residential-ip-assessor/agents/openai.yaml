interface:
  display_name: "静态家宽IP鉴定"
  short_description: "判断IP是否静态家宽住宅并生成多源交叉验证可信度报告"
  default_prompt: "Use $static-residential-ip-assessor to investigate whether an IP (or ASN+hostname+range) is static residential broadband, then produce a credible risk report with evidence, confidence, and conflicting-source analysis."
